import {  defineStyleConfig } from '@chakra-ui/react'

export const badgeTheme = defineStyleConfig({
 baseStyle: {
    borderRadius: '12px',
    padding: 2,
    width: '100%',
    textAlign: 'center'
 }
})